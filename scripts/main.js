const MODULE_ID = "foundryvtt-dnd5e-aoe-secondary";

Hooks.once("init", () => {
  // Minimal boot hook for the first module iteration.
  console.log("[AoE Secondary] module charge");
});

export { MODULE_ID };
